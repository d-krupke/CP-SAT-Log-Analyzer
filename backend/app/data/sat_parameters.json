{
 "enums": {
  "BinaryMinizationAlgorithm": {
   "BINARY_MINIMIZATION_FROM_UIP": "",
   "BINARY_MINIMIZATION_FROM_UIP_AND_DECISIONS": "",
   "NO_BINARY_MINIMIZATION": ""
  },
  "ClauseOrdering": {
   "CLAUSE_ACTIVITY": "Order clause by decreasing activity, then by increasing LBD.",
   "CLAUSE_LBD": "Order clause by increasing LBD, then by decreasing activity."
  },
  "ConflictMinimizationAlgorithm": {
   "NONE": "",
   "RECURSIVE": "",
   "SIMPLE": ""
  },
  "FPRoundingMethod": {
   "ACTIVE_LOCK_BASED": "Similar to lock based rounding except this only considers locks of active constraints from the last lp solve.",
   "LOCK_BASED": "Counts the number of linear constraints restricting the variable in the increasing values (up locks) and decreasing values (down locks). Rounds the variable in the direction of lesser locks.",
   "NEAREST_INTEGER": "Rounds to the nearest integer value.",
   "PROPAGATION_ASSISTED": "This is expensive rounding algorithm. We round variables one by one and propagate the bounds in between. If none of the rounded values fall in the continuous domain specified by lower and upper bound, we use the current lower/upper bound (whichever one is closest) instead of rounding the fractional lp solution value. If both the rounded values are in the domain, we round to nearest integer."
  },
  "MaxSatAssumptionOrder": {
   "DEFAULT_ASSUMPTION_ORDER": "",
   "ORDER_ASSUMPTION_BY_DEPTH": "",
   "ORDER_ASSUMPTION_BY_WEIGHT": ""
  },
  "MaxSatStratificationAlgorithm": {
   "STRATIFICATION_ASCENT": "Start with all literals. Each time a core is found with a given minimum weight, do not consider literals with a lower weight for the next core computation. If the subproblem is SAT, do like in STRATIFICATION_DESCENT and just add the literals with the next highest weight.",
   "STRATIFICATION_DESCENT": "Start with literals with the highest weight, and when SAT, add the literals with the next highest weight and so on.",
   "STRATIFICATION_NONE": "No stratification of the problem."
  },
  "Polarity": {
   "POLARITY_FALSE": "",
   "POLARITY_RANDOM": "",
   "POLARITY_TRUE": ""
  },
  "RestartAlgorithm": {
   "DL_MOVING_AVERAGE_RESTART": "Moving average restart based on the decision level of conflicts.",
   "FIXED_RESTART": "Fixed period restart every restart period.",
   "LBD_MOVING_AVERAGE_RESTART": "Moving average restart based on the LBD of conflicts.",
   "LUBY_RESTART": "Just follow a Luby sequence times restart_period.",
   "NO_RESTART": ""
  },
  "SearchBranching": {
   "AUTOMATIC_SEARCH": "Try to fix all literals using the underlying SAT solver's heuristics, then generate and fix literals until integer variables are fixed. New literals on integer variables are generated using the fixed search specified by the user or our default one.",
   "FIXED_SEARCH": "If used then all decisions taken by the solver are made using a fixed order as specified in the API or in the CpModelProto search_strategy field.",
   "HINT_SEARCH": "Mainly used internally. This is like FIXED_SEARCH, except we follow the solution_hint field of the CpModelProto rather than using the information provided in the search_strategy.",
   "LP_SEARCH": "If used, the solver will use heuristics from the LP relaxation. This exploit the reduced costs of the variables in the relaxation.",
   "PARTIAL_FIXED_SEARCH": "Similar to FIXED_SEARCH, but differ in how the variable not listed into the fixed search heuristics are branched on. This will always start the search tree according to the specified fixed search strategy, but will complete it using the default automatic search.",
   "PORTFOLIO_SEARCH": "Simple portfolio search used by LNS workers.",
   "PORTFOLIO_WITH_QUICK_RESTART_SEARCH": "Mainly exposed here for testing. This quickly tries a lot of randomized heuristics with a low conflict limit. It usually provides a good first solution.",
   "PSEUDO_COST_SEARCH": "If used, the solver uses the pseudo costs for branching. Pseudo costs are computed using the historical change in objective bounds when some decision are taken. Note that this works whether we use an LP or not.",
   "RANDOMIZED_SEARCH": "Randomized search. Used to increase entropy in the search."
  },
  "SharedTreeSplitStrategy": {
   "SPLIT_STRATEGY_AUTO": "Uses the default strategy, currently equivalent to SPLIT_STRATEGY_DISCREPANCY.",
   "SPLIT_STRATEGY_BALANCED_TREE": "Attempt to keep the shared tree balanced.",
   "SPLIT_STRATEGY_DISCREPANCY": "Only accept splits if the node to be split's depth+discrepancy is minimal for the desired number of leaves. The preferred child for discrepancy calculation is the one with the lowest objective lower bound or the original branch direction if the bounds are equal. This rule allows twice as many workers to work in the preferred subtree as non-preferred.",
   "SPLIT_STRATEGY_FIRST_PROPOSAL": "Workers race to split their subtree, the winner's proposal is accepted.",
   "SPLIT_STRATEGY_OBJECTIVE_LB": "Only split nodes with an objective lb equal to the global lb. If there is no objective, this is equivalent to SPLIT_STRATEGY_FIRST_PROPOSAL."
  },
  "VariableOrder": {
   "IN_ORDER": "",
   "IN_RANDOM_ORDER": "",
   "IN_REVERSE_ORDER": ""
  }
 },
 "parameters": {
  "absolute_gap_limit": {
   "default": "1e-4",
   "doc": "Stop the search when the gap between the best feasible objective (O) and\nour best objective bound (B) is smaller than a limit.\nThe exact definition is:\n- Absolute: abs(O - B)\n- Relative: abs(O - B) / max(1, abs(O)).\n\nImportant: The relative gap depends on the objective offset! If you artificially shift the objective, you will get widely different value of the relative gap.\n\nNote that if the gap is reached, the search status will be OPTIMAL. But one can check the best objective bound to see the actual gap.\n\nIf the objective is integer, then any absolute gap < 1 will lead to a true optimal. If the objective is floating point, a gap of zero make little sense so is is why we use a non-zero default value. At the end of the search, we will display a warning if OPTIMAL is reported yet the gap is greater than this absolute gap.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "double"
  },
  "add_cg_cuts": {
   "default": "true",
   "doc": "Whether we generate and add Chvatal-Gomory cuts to the LP at root node. Note that for now, this is not heavily tuned.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_clique_cuts": {
   "default": "true",
   "doc": "Whether we generate clique cuts from the binary implication graph. Note that as the search goes on, this graph will contains new binary clauses learned by the SAT engine.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_lin_max_cuts": {
   "default": "true",
   "doc": "For the lin max constraints, generates the cuts described in \"Strong mixed-integer programming formulations for trained neural networks\" by Ross Anderson et. (https://arxiv.org/pdf/1811.01988.pdf)",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_lp_constraints_lazily": {
   "default": "true",
   "doc": "If true, we start by an empty LP, and only add constraints not satisfied by the current LP solution batch by batch. A constraint that is only added like this is known as a \"lazy\" constraint in the literature, except that we currently consider all constraints as lazy here.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_mir_cuts": {
   "default": "true",
   "doc": "Whether we generate MIR cuts at root node. Note that for now, this is not heavily tuned.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_objective_cut": {
   "default": "false",
   "doc": "When the LP objective is fractional, do we add the cut that forces the linear objective expression to be greater or equal to this fractional value rounded up? We can always do that since our objective is integer, and combined with MIR heuristic to reduce the coefficient of such cut, it can help.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_rlt_cuts": {
   "default": "true",
   "doc": "Whether we generate RLT cuts. This is still experimental but can help on binary problem with a lot of clauses of size 3.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "add_zero_half_cuts": {
   "default": "true",
   "doc": "Whether we generate Zero-Half cuts at root node. Note that for now, this is not heavily tuned.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "also_bump_variables_in_conflict_reasons": {
   "default": "false",
   "doc": "When this is true, then the variables that appear in any of the reason of the variables in a conflict have their activity bumped. This is addition to the variables in the conflict, and the one that were used during conflict resolution.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "bool"
  },
  "alternative_pool_size": {
   "default": "1",
   "doc": "In order to not get stuck in local optima, when this is non-zero, we try to also work on \"older\" solutions with a worse objective value so we get a chance to follow a different LS/LNS trajectory.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "at_most_one_max_expansion_size": {
   "default": "3",
   "doc": "All at_most_one constraints with a size <= param will be replaced by a quadratic number of binary implications.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "auto_detect_greater_than_at_least_one_of": {
   "default": "true",
   "doc": "If true, then the precedences propagator try to detect for each variable if it has a set of \"optional incoming arc\" for which at least one of them is present. This is usually useful to have but can be slow on model with a lot of precedence.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "binary_minimization_algorithm": {
   "default": "BINARY_MINIMIZATION_FROM_UIP_AND_DECISIONS",
   "doc": "",
   "enum": "BinaryMinizationAlgorithm",
   "repeated": false,
   "section": "Conflict analysis",
   "type": "BinaryMinizationAlgorithm"
  },
  "binary_search_num_conflicts": {
   "default": "-1",
   "doc": "If non-negative, perform a binary search on the objective variable in order to find an [min, max] interval outside of which the solver proved unsat/sat under this amount of conflict. This can quickly reduce the objective domain on some problems.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "blocking_restart_multiplier": {
   "default": "1.4",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "double"
  },
  "blocking_restart_window_size": {
   "default": "5000",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "int32"
  },
  "boolean_encoding_level": {
   "default": "1",
   "doc": "A non-negative level indicating how much we should try to fully encode Integer variables as Boolean.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "catch_sigint_signal": {
   "default": "true",
   "doc": "Indicates if the CP-SAT layer should catch Control-C (SIGINT) signals when calling solve. If set, catching the SIGINT signal will terminate the search gracefully, as if a time limit was reached.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "check_drat_proof": {
   "default": "false",
   "doc": "If true, and if the problem is UNSAT, a DRAT proof of this UNSAT property\nis checked after the solver has finished. As of November 2025, this only\nworks for pure SAT problems, with\n - num_workers = 1,\n - cp_model_presolve = false,\n - linearization_level <= 1,\n - symmetry_level <= 1.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "bool"
  },
  "check_lrat_proof": {
   "default": "false",
   "doc": "If true, inferred clauses are checked with an LRAT checker as they are\nlearned, in presolve (reduced to trivial simplifications if\ncp_model_presolve is false), and in each worker. As of December 2025, this\nonly works with pure SAT problems, with\n - cp_model_presolve = false,\n - linearization_level <= 1,\n - symmetry_level <= 1.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "bool"
  },
  "check_merged_lrat_proof": {
   "default": "false",
   "doc": "If true, and if output_lrat_proof is true and the problem is UNSAT, check that the merged proof file is valid, i.e., that clause sharing between workers is correct. This checks each inferred clause, so you might want to disable check_lrat_proof to avoid redundant work. As of November 2025, this only works for pure SAT problems, with num_workers = 1.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "bool"
  },
  "chronological_backtrack_min_conflicts": {
   "default": "1000",
   "doc": "If chronological backtracking is enabled, this is the minimum number of conflicts before we will consider backjumping.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "int32"
  },
  "clause_activity_decay": {
   "default": "0.999",
   "doc": "Clause activity parameters (same effect as the one on the variables).",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "clause_cleanup_lbd_bound": {
   "default": "5",
   "doc": "All the clauses with a LBD (literal blocks distance) lower or equal to this parameters will always be kept.\n\nNote that the LBD of a clause that just propagated is 1 + number of different decision levels of its literals. So that the \"classic\" LBD of a learned conflict is the same as its LBD when we backjump and then propagate it.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "clause_cleanup_lbd_tier1": {
   "default": "0",
   "doc": "All the clause with a LBD lower or equal to this will be kept except if its activity hasn't been bumped in the last 32 cleanup phase. Note that this has no effect if it is <= clause_cleanup_lbd_bound.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "clause_cleanup_lbd_tier2": {
   "default": "0",
   "doc": "All the clause with a LBD lower or equal to this will be kept except if its activity hasn't been bumped since the previous cleanup phase. Note that this has no effect if it is <= clause_cleanup_lbd_bound or <= clause_cleanup_lbd_tier1.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "clause_cleanup_ordering": {
   "default": "CLAUSE_ACTIVITY",
   "doc": "",
   "enum": "ClauseOrdering",
   "repeated": false,
   "section": "Clause database management",
   "type": "ClauseOrdering"
  },
  "clause_cleanup_period": {
   "default": "10000",
   "doc": "Trigger a cleanup when this number of \"deletable\" clauses is learned.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "clause_cleanup_period_increment": {
   "default": "0",
   "doc": "Increase clause_cleanup_period by this amount after each cleanup.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "clause_cleanup_ratio": {
   "default": "0.5",
   "doc": "During a cleanup, if clause_cleanup_target is 0, we will delete the clause_cleanup_ratio of \"deletable\" clauses instead of aiming for a fixed target of clauses to keep.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "double"
  },
  "clause_cleanup_target": {
   "default": "0",
   "doc": "During a cleanup, we will always keep that number of \"deletable\" clauses. Note that this doesn't include the \"protected\" clauses.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "convert_intervals": {
   "default": "true",
   "doc": "Temporary flag util the feature is more mature. This convert intervals to the newer proto format that support affine start/var/end instead of just variables.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "core_minimization_level": {
   "default": "2",
   "doc": "If positive, we spend some effort on each core:\n- At level 1, we use a simple heuristic to try to minimize an UNSAT core.\n- At level 2, we use propagation to minimize the core but also identify\n  literal in at most one relationship in this core.",
   "enum": null,
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "int32"
  },
  "count_assumption_levels_in_lbd": {
   "default": "true",
   "doc": "Whether or not the assumption levels are taken into account during the LBD computation. According to the reference below, not counting them improves the solver in some situation. Note that this only impact solves under assumptions.\n\nGilles Audemard, Jean-Marie Lagniez, Laurent Simon, \"Improving Glucose for Incremental SAT Solving with Assumptions: Application to MUS Extraction\" Theory and Applications of Satisfiability Testing - SAT 2013, Lecture Notes in Computer Science Volume 7962, 2013, pp 309-317.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "cover_optimization": {
   "default": "true",
   "doc": "If true, when the max-sat algo find a core, we compute the minimal number of literals in the core that needs to be true to have a feasible solution. This is also called core exhaustion in more recent max-SAT papers.",
   "enum": null,
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "bool"
  },
  "cp_model_presolve": {
   "default": "true",
   "doc": "Whether we presolve the cp_model before solving it.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "cp_model_probing_level": {
   "default": "2",
   "doc": "How much effort do we spend on probing. 0 disables it completely.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "cp_model_use_sat_presolve": {
   "default": "true",
   "doc": "Whether we also use the sat presolve when cp_model_presolve is true.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "create_1uip_boolean_during_icr": {
   "default": "true",
   "doc": "If true, and during integer conflict resolution (icr) the 1-UIP is an integer literal for which we do not have an associated Boolean. Create one.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "cut_active_count_decay": {
   "default": "0.8",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "cut_cleanup_target": {
   "default": "1000",
   "doc": "Target number of constraints to remove during cleanup.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "cut_level": {
   "default": "1",
   "doc": "Control the global cut effort. Zero will turn off all cut. For now we just have one level. Note also that most cuts are only used at linearization level >= 2.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "cut_max_active_count_value": {
   "default": "1e10",
   "doc": "These parameters are similar to sat clause management activity parameters. They are effective only if the number of generated cuts exceed the storage limit. Default values are based on a few experiments on miplib instances.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "debug_crash_if_lrat_check_fails": {
   "default": "false",
   "doc": "Crash if the LRAT UNSAT proof is invalid.",
   "enum": null,
   "repeated": false,
   "section": "Debugging parameters",
   "type": "bool"
  },
  "debug_crash_if_presolve_breaks_hint": {
   "default": "false",
   "doc": "Crash if presolve breaks a feasible hint.",
   "enum": null,
   "repeated": false,
   "section": "Debugging parameters",
   "type": "bool"
  },
  "debug_crash_on_bad_hint": {
   "default": "false",
   "doc": "Crash if we do not manage to complete the hint into a full solution.",
   "enum": null,
   "repeated": false,
   "section": "Debugging parameters",
   "type": "bool"
  },
  "debug_max_num_presolve_operations": {
   "default": "0",
   "doc": "If positive, try to stop just after that many presolve rules have been applied. This is mainly useful for debugging presolve.",
   "enum": null,
   "repeated": false,
   "section": "Debugging parameters",
   "type": "int32"
  },
  "debug_postsolve_with_full_solver": {
   "default": "false",
   "doc": "We have two different postsolve code. The default one should be better and it allows for a more powerful presolve, but it can be useful to postsolve using the full solver instead.",
   "enum": null,
   "repeated": false,
   "section": "Debugging parameters",
   "type": "bool"
  },
  "decision_subsumption_during_conflict_analysis": {
   "default": "true",
   "doc": "Try even more subsumption options during conflict analysis.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "bool"
  },
  "default_restart_algorithms": {
   "default": "LUBY_RESTART,LBD_MOVING_AVERAGE_RESTART,DL_MOVING_AVERAGE_RESTART",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "string"
  },
  "detect_linearized_product": {
   "default": "false",
   "doc": "Infer products of Boolean or of Boolean time IntegerVariable from the linear constrainst in the problem. This can be used in some cuts, altough for now we don't really exploit it.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "detect_table_with_cost": {
   "default": "false",
   "doc": "If true, we detect variable that are unique to a table constraint and only there to encode a cost on each tuple. This is usually the case when a WCSP (weighted constraint program) is encoded into CP-SAT format.\n\nThis can lead to a dramatic speed-up for such problems but is still experimental at this point.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "disable_constraint_expansion": {
   "default": "false",
   "doc": "If true, it disable all constraint expansion. This should only be used to test the presolve of expanded constraints.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "diversify_lns_params": {
   "default": "false",
   "doc": "If true, registers more lns subsolvers with different parameters.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "eagerly_subsume_last_n_conflicts": {
   "default": "4",
   "doc": "If >=0, each time we have a conflict, we try to subsume the last n learned clause with it.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "int32"
  },
  "encode_complex_linear_constraint_with_integer": {
   "default": "false",
   "doc": "Linear constraint with a complex right hand side (more than a single interval) need to be expanded, there is a couple of way to do that.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "encode_cumulative_as_reservoir": {
   "default": "false",
   "doc": "Encore cumulative with fixed demands and capacity as a reservoir constraint. The only reason you might want to do that is to test the reservoir propagation code!",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "enumerate_all_solutions": {
   "default": "false",
   "doc": "Whether we enumerate all solutions of a problem without objective.\n\nWARNING:\n- This can be used with num_workers > 1 but then each solutions can be\n  found more than once, so it is up to the client to deduplicate them.\n- If keep_all_feasible_solutions_in_presolve is unset, we will set it to\n  true as otherwise, many feasible solution can just be removed by the\n  presolve. It is still possible to manually set this to false if one only\n  wants to enumerate all solutions of the presolved model.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "expand_alldiff_constraints": {
   "default": "false",
   "doc": "If true, expand all_different constraints that are not permutations. Permutations (#Variables = #Values) are always expanded.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "expand_reservoir_constraints": {
   "default": "true",
   "doc": "If true, expand the reservoir constraints by creating booleans for all possible precedences between event and encoding the constraint.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "expand_reservoir_using_circuit": {
   "default": "false",
   "doc": "Mainly useful for testing.\n\nIf this and expand_reservoir_constraints is true, we use a different encoding of the reservoir constraint using circuit instead of precedences. Note that this is usually slower, but can exercise different part of the solver. Note that contrary to the precedence encoding, this easily support variable demands.\n\nWARNING: with this encoding, the constraint takes a slightly different meaning. There must exist a permutation of the events occurring at the same time such that the level is within the reservoir after each of these events (in this permuted order). So we cannot have +100 and -100 at the same time if the level must be between 0 and 10 (as authorized by the reservoir constraint).",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "exploit_all_lp_solution": {
   "default": "true",
   "doc": "If true and the Lp relaxation of the problem has a solution, try to exploit it. This is same as above except in this case the lp solution might not be an integer solution.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "exploit_all_precedences": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "exploit_best_solution": {
   "default": "false",
   "doc": "When branching on a variable, follow the last best solution value.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "exploit_integer_lp_solution": {
   "default": "true",
   "doc": "If true and the Lp relaxation of the problem has an integer optimal solution, try to exploit it. Note that since the LP relaxation may not contain all the constraints, such a solution is not necessarily a solution of the full problem.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "exploit_objective": {
   "default": "true",
   "doc": "When branching an a variable that directly affect the objective, branch on the value that lead to the best objective first.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "exploit_relaxation_solution": {
   "default": "false",
   "doc": "When branching on a variable, follow the last best relaxation solution value. We use the relaxation with the tightest bound on the objective as the best relaxation solution.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "extra_subsolvers": {
   "default": null,
   "doc": "A convenient way to add more workers types. These will be added at the beginning of the list.",
   "enum": null,
   "repeated": true,
   "section": "Multithread",
   "type": "string"
  },
  "extra_subsumption_during_conflict_analysis": {
   "default": "true",
   "doc": "It is possible that \"intermediate\" clauses during conflict resolution subsumes some of the clauses that propagated. This is quite cheap to detect and result in more subsumption/strengthening of clauses.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "bool"
  },
  "feasibility_jump_batch_dtime": {
   "default": "0.1",
   "doc": "How much dtime for each LS batch.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "feasibility_jump_decay": {
   "default": "0.95",
   "doc": "On each restart, we randomly choose if we use decay (with this parameter) or no decay.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "feasibility_jump_enable_restarts": {
   "default": "true",
   "doc": "When stagnating, feasibility jump will either restart from a default solution (with some possible randomization), or randomly pertubate the current solution. This parameter selects the first option.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "feasibility_jump_linearization_level": {
   "default": "2",
   "doc": "How much do we linearize the problem in the local search code.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "feasibility_jump_max_expanded_constraint_size": {
   "default": "500",
   "doc": "Maximum size of no_overlap or no_overlap_2d constraint for a quadratic expansion. This might look a lot, but by expanding such constraint, we get a linear time evaluation per single variable moves instead of a slow O(n log n) one.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "feasibility_jump_restart_factor": {
   "default": "1",
   "doc": "This is a factor that directly influence the work before each restart. Increasing it leads to longer restart.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "feasibility_jump_var_perburbation_range_ratio": {
   "default": "0.2",
   "doc": "Max distance between the default value and the pertubated value relative to the range of the domain of the variable.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "feasibility_jump_var_randomization_probability": {
   "default": "0.05",
   "doc": "Probability for a variable to have a non default value upon restarts or perturbations.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "fill_additional_solutions_in_response": {
   "default": "false",
   "doc": "If true, the final response addition_solutions field will be filled with all solutions from our solutions pool.\n\nNote that if both this field and enumerate_all_solutions is true, we will copy to the pool all of the solution found. So if solution_pool_size is big enough, you can get all solutions this way instead of using the solution callback.\n\nNote that this only affect the \"final\" solution, not the one passed to the solution callbacks.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "fill_tightened_domains_in_response": {
   "default": "false",
   "doc": "If true, add information about the derived variable domains to the CpSolverResponse. It is an option because it makes the response slighly bigger and there is a bit more work involved during the postsolve to construct it, but it should still have a low overhead. See the tightened_variables field in CpSolverResponse for more details.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "filter_sat_postsolve_clauses": {
   "default": "false",
   "doc": "Internal parameter. During BVE, if we eliminate a variable x, by default we will push all clauses containing x and all clauses containing not(x) to the postsolve. However, it is possible to write the postsolve code so that only one such set is needed. The idea is that, if we push the set containing a literal l, is to set l to false except if it is needed to satisfy one of the clause in the set. This is always beneficial, but for historical reason, not all our postsolve algorithm support this.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "filter_subsolvers": {
   "default": null,
   "doc": "",
   "enum": null,
   "repeated": true,
   "section": "Multithread",
   "type": "string"
  },
  "find_big_linear_overlap": {
   "default": "true",
   "doc": "Try to find large \"rectangle\" in the linear constraint matrix with identical lines. If such rectangle is big enough, we can introduce a new integer variable corresponding to the common expression and greatly reduce the number of non-zero.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "find_clauses_that_are_exactly_one": {
   "default": "true",
   "doc": "By propagating (or just using binary clauses), one can detect that all literal of a clause are actually in at most one relationship. Thus this constraint can be promoted to an exactly one constraints. This should help as it convey more structure. Note that this is expensive, so we have a deterministic limit in place.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "find_multiple_cores": {
   "default": "true",
   "doc": "Whether we try to find more independent cores for a given set of assumptions in the core based max-SAT algorithms.",
   "enum": null,
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "bool"
  },
  "fix_variables_to_their_hinted_value": {
   "default": "false",
   "doc": "If true, variables appearing in the solution hints will be fixed to their hinted value.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "fp_rounding": {
   "default": "PROPAGATION_ASSISTED",
   "doc": "",
   "enum": "FPRoundingMethod",
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "FPRoundingMethod"
  },
  "glucose_decay_increment": {
   "default": "0.01",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "glucose_decay_increment_period": {
   "default": "5000",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "int32"
  },
  "glucose_max_decay": {
   "default": "0.95",
   "doc": "The activity starts at 0.8 and increment by 0.01 every 5000 conflicts until 0.95. This \"hack\" seems to work well and comes from:\n\nGlucose 2.3 in the SAT 2013 Competition - SAT Competition 2013 http://edacc4.informatik.uni-ulm.de/SC13/solver-description-download/136",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "hint_conflict_limit": {
   "default": "10",
   "doc": "Conflict limit used in the phase that exploit the solution hint.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "ignore_names": {
   "default": "true",
   "doc": "If true, we don't keep names in our internal copy of the user given model.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "ignore_subsolvers": {
   "default": null,
   "doc": "Rather than fully specifying subsolvers, it is often convenient to just remove the ones that are not useful on a given problem or only keep specific ones for testing. Each string is interpreted as a \"glob\", so we support '*' and '?'.\n\nThe way this work is that we will only accept a name that match a filter pattern (if non-empty) and do not match an ignore pattern. Note also that these fields work on LNS or LS names even if these are currently not specified via the subsolvers field.",
   "enum": null,
   "repeated": true,
   "section": "Multithread",
   "type": "string"
  },
  "infer_all_diffs": {
   "default": "true",
   "doc": "Run a max-clique code amongst all the x != y we can find and try to infer set of variables that are all different. This allows to close neos16.mps for instance. Note that we only run this code if there is no all_diff already in the model so that if a user want to add some all_diff, we assume it is well done and do not try to add more.\n\nThis will also detect and add no_overlap constraints, if all the relations x != y have \"offsets\" between them. I.e. x > y + offset.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "initial_polarity": {
   "default": "POLARITY_FALSE",
   "doc": "",
   "enum": "Polarity",
   "repeated": false,
   "section": "Branching and polarity",
   "type": "Polarity"
  },
  "initial_variables_activity": {
   "default": "0.0",
   "doc": "The initial value of the variables activity. A non-zero value only make sense when use_erwa_heuristic is true. Experiments with a value of 1e-2 together with the ERWA heuristic showed slighthly better result than simply using zero. The idea is that when the \"learning rate\" of a variable becomes lower than this value, then we prefer to branch on never explored before variables. This is not in the ERWA paper.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "double"
  },
  "inprocessing_dtime_ratio": {
   "default": "0.2",
   "doc": "Proportion of deterministic time we should spend on inprocessing. At each \"restart\", if the proportion is below this ratio, we will do some inprocessing, otherwise, we skip it for this restart.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "double"
  },
  "inprocessing_minimization_dtime": {
   "default": "1.0",
   "doc": "Parameters for an heuristic similar to the one described in \"An effective learnt clause minimization approach for CDCL Sat Solvers\", https://www.ijcai.org/proceedings/2017/0098.pdf\n\nThis is the amount of dtime we should spend on this technique during each inprocessing phase.\n\nThe minimization technique is the same as the one used to minimize core in max-sat. We also minimize problem clauses and not just the learned clause that we keep forever like in the paper.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "double"
  },
  "inprocessing_minimization_use_all_orderings": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "bool"
  },
  "inprocessing_minimization_use_conflict_analysis": {
   "default": "true",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "bool"
  },
  "inprocessing_probing_dtime": {
   "default": "1.0",
   "doc": "The amount of dtime we should spend on probing for each inprocessing round.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "double"
  },
  "inprocessing_use_congruence_closure": {
   "default": "true",
   "doc": "Whether we use the algorithm described in \"Clausal Congruence closure\", Armin Biere, Katalin Fazekas, Mathias Fleury, Nils Froleyks, 2024.\n\nNote that we only have a basic version currently.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "bool"
  },
  "inprocessing_use_sat_sweeping": {
   "default": "false",
   "doc": "Whether we use the SAT sweeping algorithm described in \"Clausal Equivalence Sweeping\", Armin Biere, Katalin Fazekas, Mathias Fleury, Nils Froleyks, 2025.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "bool"
  },
  "instantiate_all_variables": {
   "default": "true",
   "doc": "If true, the solver will add a default integer branching strategy to the already defined search strategy. If not, some variable might still not be fixed at the end of the search. For now we assume these variable can just be set to their lower bound.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "interleave_batch_size": {
   "default": "0",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "int32"
  },
  "interleave_search": {
   "default": "false",
   "doc": "Experimental. If this is true, then we interleave all our major search strategy and distribute the work amongst num_workers.\n\nThe search is deterministic (independently of num_workers!), and we schedule and wait for interleave_batch_size task to be completed before synchronizing and scheduling the next batch of tasks.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "keep_all_feasible_solutions_in_presolve": {
   "default": "false",
   "doc": "If true, we disable the presolve reductions that remove feasible solutions from the search space. Such solution are usually dominated by a \"better\" solution that is kept, but depending on the situation, we might want to keep all solutions.\n\nA trivial example is when a variable is unused. If this is true, then the presolve will not fix it to an arbitrary value and it will stay in the search space.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "keep_symmetry_in_presolve": {
   "default": "false",
   "doc": "Experimental. This will compute the symmetry of the problem once and for all. All presolve operations we do should keep the symmetry group intact or modify it properly. For now we have really little support for this. We will disable a bunch of presolve operations that could be supported.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "lb_relax_num_workers_threshold": {
   "default": "16",
   "doc": "Only use lb-relax if we have at least that many workers.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "linear_split_size": {
   "default": "100",
   "doc": "Linear constraints that are not pseudo-Boolean and that are longer than this size will be split into sqrt(size) intermediate sums in order to have faster propation in the CP engine.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "linearization_level": {
   "default": "1",
   "doc": "A non-negative level indicating the type of constraints we consider in the LP relaxation. At level zero, no LP relaxation is used. At level 1, only the linear constraint and full encoding are added. At level 2, we also add all the Boolean constraints.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "lns_initial_deterministic_limit": {
   "default": "0.1",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "lns_initial_difficulty": {
   "default": "0.5",
   "doc": "Initial parameters for neighborhood generation.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "load_at_most_ones_in_sat_presolve": {
   "default": "false",
   "doc": "If we try to load at most ones and exactly ones constraints when running the pure SAT presolve. Or if we just ignore them.\n\nIf one detects at_most_one via merge_at_most_one_work_limit or exactly one with find_clauses_that_are_exactly_one, it might be good to also set this to true.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "log_prefix": {
   "default": "",
   "doc": "Add a prefix to all logs.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "string"
  },
  "log_search_progress": {
   "default": "false",
   "doc": "Whether the solver should log the search progress. This is the maing logging parameter and if this is false, none of the logging (callbacks, log_to_stdout, log_to_response, ...) will do anything.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "log_subsolver_statistics": {
   "default": "false",
   "doc": "Whether the solver should display per sub-solver search statistics. This is only useful is log_search_progress is set to true, and if the number of search workers is > 1. Note that in all case we display a bit of stats with one line per subsolver.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "log_to_response": {
   "default": "false",
   "doc": "Log to response proto.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "log_to_stdout": {
   "default": "true",
   "doc": "Log to stdout.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "lp_dual_tolerance": {
   "default": "1e-7",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "lp_primal_tolerance": {
   "default": "1e-7",
   "doc": "The internal LP tolerances used by CP-SAT. These applies to the internal and scaled problem. If the domains of your variables are large it might be good to use lower tolerances. If your problem is binary with low coefficients, it might be good to use higher ones to speed-up the lp solves.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "max_all_diff_cut_size": {
   "default": "64",
   "doc": "Cut generator for all diffs can add too many cuts for large all_diff constraints. This parameter restricts the large all_diff constraints to have a cut generator.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_alldiff_domain_size": {
   "default": "256",
   "doc": "Max domain size for all_different constraints to be expanded.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "max_backjump_levels": {
   "default": "50",
   "doc": "If chronological backtracking is enabled, this is the maximum number of levels we will backjump over, otherwise we will backtrack.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "int32"
  },
  "max_clause_activity_value": {
   "default": "1e20",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "max_consecutive_inactive_count": {
   "default": "100",
   "doc": "If a constraint/cut in LP is not active for that many consecutive OPTIMAL solves, remove it from the LP. Note that it might be added again later if it become violated by the current LP solution.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_cut_rounds_at_level_zero": {
   "default": "1",
   "doc": "Max number of time we perform cut generation and resolve the LP at level 0.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_deterministic_time": {
   "default": "inf",
   "doc": "Maximum time allowed in deterministic time to solve a problem. The deterministic time should be correlated with the real time used by the solver, the time unit being as close as possible to a second.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "double"
  },
  "max_domain_size_for_linear2_expansion": {
   "default": "8",
   "doc": "Max domain size for expanding linear2 constraints (ax + by ==/!= c).",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "max_domain_size_when_encoding_eq_neq_constraints": {
   "default": "16",
   "doc": "When loading a*x + b*y ==/!= c when x and y are both fully encoded. The solver may decide to replace the linear equation by a set of clauses. This is triggered if the sizes of the domains of x and y are below the threshold.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_drat_time_in_seconds": {
   "default": "inf",
   "doc": "The maximum time allowed to check the DRAT proof (this can take more time than the solve itself). Only used if check_drat_proof is true.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "double"
  },
  "max_integer_rounding_scaling": {
   "default": "600",
   "doc": "In the integer rounding procedure used for MIR and Gomory cut, the maximum \"scaling\" we use (must be positive). The lower this is, the lower the integer coefficients of the cut will be. Note that cut generated by lower values are not necessarily worse than cut generated by larger value. There is no strict dominance relationship.\n\nSetting this to 2 result in the \"strong fractional rouding\" of Letchford and Lodi.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_lin_max_size_for_expansion": {
   "default": "0",
   "doc": "If the number of expressions in the lin_max is less that the max size parameter, model expansion replaces target = max(xi) by linear constraint with the introduction of new booleans bi such that bi => target == xi.\n\nThis is mainly for experimenting compared to a custom lin_max propagator.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "max_memory_in_mb": {
   "default": "10000",
   "doc": "Maximum memory allowed for the whole thread containing the solver. The solver will abort as soon as it detects that this limit is crossed. As a result, this limit is approximative, but usually the solver will not go too much over.\n\nTODO(user): This is only used by the pure SAT solver, generalize to CP-SAT.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "int64"
  },
  "max_num_cuts": {
   "default": "10000",
   "doc": "The limit on the number of cuts in our cut pool. When this is reached we do not generate cuts anymore.\n\nTODO(user): We should probably remove this parameters, and just always generate cuts but only keep the best n or something.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "max_num_deterministic_batches": {
   "default": "0",
   "doc": "Stops after that number of batches has been scheduled. This only make sense when interleave_search is true.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "int32"
  },
  "max_num_intervals_for_timetable_edge_finding": {
   "default": "100",
   "doc": "Max number of intervals for the timetable_edge_finding algorithm to propagate. A value of 0 disables the constraint.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "max_number_of_conflicts": {
   "default": "0x7FFFFFFFFFFFFFFF",
   "doc": "Maximum number of conflicts allowed to solve a problem.\n\nTODO(user): Maybe change the way the conflict limit is enforced? currently it is enforced on each independent internal SAT solve, rather than on the overall number of conflicts across all solves. So in the context of an optimization problem, this is not really usable directly by a client.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "int64"
  },
  "max_pairs_pairwise_reasoning_in_no_overlap_2d": {
   "default": "1250",
   "doc": "If the number of pairs to look is below this threshold, do an extra step of propagation in the no_overlap_2d constraint by looking at all pairs of intervals.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "max_presolve_iterations": {
   "default": "3",
   "doc": "In case of large reduction in a presolve iteration, we perform multiple presolve iterations. This parameter controls the maximum number of such presolve iterations.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "max_sat_assumption_order": {
   "default": "DEFAULT_ASSUMPTION_ORDER",
   "doc": "",
   "enum": "MaxSatAssumptionOrder",
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "MaxSatAssumptionOrder"
  },
  "max_sat_reverse_assumption_order": {
   "default": "false",
   "doc": "If true, adds the assumption in the reverse order of the one defined by max_sat_assumption_order.",
   "enum": null,
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "bool"
  },
  "max_sat_stratification": {
   "default": "STRATIFICATION_DESCENT",
   "doc": "",
   "enum": "MaxSatStratificationAlgorithm",
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "MaxSatStratificationAlgorithm"
  },
  "max_size_to_create_precedence_literals_in_disjunctive": {
   "default": "60",
   "doc": "Create one literal for each disjunction of two pairs of tasks. This slows down the solve time, but improves the lower bound of the objective in the makespan case. This will be triggered if the number of intervals is less or equal than the parameter and if use_strong_propagation_in_disjunctive is true.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "max_time_in_seconds": {
   "default": "inf",
   "doc": "Maximum time allowed in seconds to solve a problem. The counter will starts at the beginning of the Solve() call.",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "double"
  },
  "max_variable_activity_value": {
   "default": "1e100",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "maximum_regions_to_split_in_disconnected_no_overlap_2d": {
   "default": "0",
   "doc": "Detects when the space where items of a no_overlap_2d constraint can placed is disjoint (ie., fixed boxes split the domain). When it is the case, we can introduce a boolean for each pair <item, component> encoding whether the item is in the component or not. Then we replace the original no_overlap_2d constraint by one no_overlap_2d constraint for each component, with the new booleans as the enforcement_literal of the intervals. This is equivalent to expanding the original no_overlap_2d constraint into a bin packing problem with each connected component being a bin. This heuristic is only done when the number of regions to split is less than this parameter and <= 1 disables it.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "merge_at_most_one_work_limit": {
   "default": "1e8",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "double"
  },
  "merge_no_overlap_work_limit": {
   "default": "1e12",
   "doc": "During presolve, we use a maximum clique heuristic to merge together no-overlap constraints or at most one constraints. This code can be slow, so we have a limit in place on the number of explored nodes in the underlying graph. The internal limit is an int64, but we use double here to simplify manual input.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "double"
  },
  "min_orthogonality_for_lp_constraints": {
   "default": "0.05",
   "doc": "While adding constraints, skip the constraints which have orthogonality less than 'min_orthogonality_for_lp_constraints' with already added constraints during current call. Orthogonality is defined as 1 - cosine(vector angle between constraints). A value of zero disable this feature.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "minimization_algorithm": {
   "default": "RECURSIVE",
   "doc": "",
   "enum": "ConflictMinimizationAlgorithm",
   "repeated": false,
   "section": "Conflict analysis",
   "type": "ConflictMinimizationAlgorithm"
  },
  "minimize_reduction_during_pb_resolution": {
   "default": "false",
   "doc": "A different algorithm during PB resolution. It minimizes the number of calls to ReduceCoefficients() which can be time consuming. However, the search space will be different and if the coefficients are large, this may lead to integer overflows that could otherwise be prevented.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "minimize_shared_clauses": {
   "default": "true",
   "doc": "Minimize and detect subsumption of shared clauses immediately after they are imported.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "mip_automatically_scale_variables": {
   "default": "true",
   "doc": "If true, some continuous variable might be automatically scaled. For now, this is only the case where we detect that a variable is actually an integer multiple of a constant. For instance, variables of the form k * 0.5 are quite frequent, and if we detect this, we will scale such variable domain by 2 to make it implied integer.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "mip_check_precision": {
   "default": "1e-4",
   "doc": "As explained in mip_precision and mip_max_activity_exponent, we cannot always reach the wanted precision during scaling. We use this threshold to enphasize in the logs when the precision seems bad.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "mip_compute_true_objective_bound": {
   "default": "true",
   "doc": "Even if we make big error when scaling the objective, we can always derive a correct lower bound on the original objective by using the exact lower bound on the scaled integer version of the objective. This should be fast, but if you don't care about having a precise lower bound, you can turn it off.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "mip_drop_tolerance": {
   "default": "1e-16",
   "doc": "Any value in the input mip with a magnitude lower than this will be set to zero. This is to avoid some issue in LP presolving.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "mip_max_activity_exponent": {
   "default": "53",
   "doc": "To avoid integer overflow, we always force the maximum possible constraint activity (and objective value) according to the initial variable domain to be smaller than 2 to this given power. Because of this, we cannot always reach the \"mip_wanted_precision\" parameter above.\n\nThis can go as high as 62, but some internal algo currently abort early if they might run into integer overflow, so it is better to keep it a bit lower than this.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "mip_max_bound": {
   "default": "1e7",
   "doc": "We need to bound the maximum magnitude of the variables for CP-SAT, and that is the bound we use. If the MIP model expect larger variable value in the solution, then the converted model will likely not be relevant.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "mip_max_valid_magnitude": {
   "default": "1e20",
   "doc": "Any finite values in the input MIP must be below this threshold, otherwise the model will be reported invalid. This is needed to avoid floating point overflow when evaluating bounds * coeff for instance. We are a bit more defensive, but in practice, users shouldn't use super large values in a MIP.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "mip_presolve_level": {
   "default": "2",
   "doc": "When solving a MIP, we do some basic floating point presolving before scaling the problem to integer to be handled by CP-SAT. This control how much of that presolve we do. It can help to better scale floating point model, but it is not always behaving nicely.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "mip_scale_large_domain": {
   "default": "false",
   "doc": "If this is false, then mip_var_scaling is only applied to variables with \"small\" domain. If it is true, we scale all floating point variable independenlty of their domain.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "mip_treat_high_magnitude_bounds_as_infinity": {
   "default": "false",
   "doc": "By default, any variable/constraint bound with a finite value and a magnitude greater than the mip_max_valid_magnitude will result with a invalid model. This flags change the behavior such that such bounds are silently transformed to +\u221e or -\u221e.\n\nIt is recommended to keep it at false, and create valid bounds.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "mip_var_scaling": {
   "default": "1.0",
   "doc": "All continuous variable of the problem will be multiplied by this factor. By default, we don't do any variable scaling and rely on the MIP model to specify continuous variable domain with the wanted precision.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "mip_wanted_precision": {
   "default": "1e-6",
   "doc": "When scaling constraint with double coefficients to integer coefficients, we will multiply by a power of 2 and round the coefficients. We will choose the lowest power such that we have no potential overflow (see mip_max_activity_exponent) and the worst case constraint activity error does not exceed this threshold.\n\nNote that we also detect constraint with rational coefficients and scale them accordingly when it seems better instead of using a power of 2.\n\nWe also relax all constraint bounds by this absolute value. For pure integer constraint, if this value if lower than one, this will not change anything. However it is needed when scaling MIP problems.\n\nIf we manage to scale a constraint correctly, the maximum error we can make will be twice this value (once for the scaling error and once for the relaxed bounds). If we are not able to scale that well, we will display that fact but still scale as best as we can.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "double"
  },
  "new_constraints_batch_size": {
   "default": "50",
   "doc": "Add that many lazy constraints (or cuts) at once in the LP. Note that at the beginning of the solve, we do add more than this.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "new_linear_propagation": {
   "default": "true",
   "doc": "The new linear propagation code treat all constraints at once and use an adaptation of Bellman-Ford-Tarjan to propagate constraint in a smarter order and potentially detect propagation cycle earlier.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "no_overlap_2d_boolean_relations_limit": {
   "default": "10",
   "doc": "If less than this number of boxes are present in a no-overlap 2d, we\ncreate 4 Booleans per pair of boxes:\n- Box 2 is after Box 1 on x.\n- Box 1 is after Box 2 on x.\n- Box 2 is after Box 1 on y.\n- Box 1 is after Box 2 on y.\n\nNote that at least one of them must be true, and at most one on x and one on y can be true.\n\nThis can significantly help in closing small problem. The SAT reasoning can be a lot more powerful when we take decision on such positional relations.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "num_conflicts_before_strategy_changes": {
   "default": "0",
   "doc": "After each restart, if the number of conflict since the last strategy change is greater that this, then we increment a \"strategy_counter\" that can be use to change the search strategy used by the following restarts.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "int32"
  },
  "num_full_subsolvers": {
   "default": "0",
   "doc": "We distinguish subsolvers that consume a full thread, and the ones that are always interleaved. If left at zero, we will fix this with a default formula that depends on num_workers. But if you start modifying what runs, you might want to fix that to a given value depending on the num_workers you use.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "int32"
  },
  "num_search_workers": {
   "default": "0",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "int32"
  },
  "num_violation_ls": {
   "default": "0",
   "doc": "This will create incomplete subsolvers (that are not LNS subsolvers) that use the feasibility jump code to find improving solution, treating the objective improvement as a hard constraint.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "num_workers": {
   "default": "0",
   "doc": "Specify the number of parallel workers (i.e. threads) to use during search. This should usually be lower than your number of available cpus + hyperthread in your machine.\n\nA value of 0 means the solver will try to use all cores on the machine. A number of 1 means no parallelism.\n\nNote that 'num_workers' is the preferred name, but if it is set to zero, we will still read the deprecated 'num_search_workers'.\n\nAs of 2020-04-10, if you're using SAT via MPSolver (to solve integer programs) this field is overridden with a value of 8, if the field is not set *explicitly*. Thus, always set this field explicitly or via MPSolver::SetNumThreads().",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "int32"
  },
  "only_add_cuts_at_level_zero": {
   "default": "false",
   "doc": "For the cut that can be generated at any level, this control if we only try to generate them at the root node.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "only_solve_ip": {
   "default": "false",
   "doc": "If one try to solve a MIP model with CP-SAT, because we assume all variable to be integer after scaling, we will not necessarily have the correct optimal. Note however that all feasible solutions are valid since we will just solve a more restricted version of the original problem.\n\nThis parameters is here to prevent user to think the solution is optimal when it might not be. One will need to manually set this to false to solve a MIP model where the optimal might be different.\n\nNote that this is tested after some MIP presolve steps, so even if not all original variable are integer, we might end up with a pure IP after presolve and after implied integer detection.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "optimize_with_core": {
   "default": "false",
   "doc": "The default optimization method is a simple \"linear scan\", each time trying to find a better solution than the previous one. If this is true, then we use a core-based approach (like in max-SAT) when we try to increase the lower bound instead.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "optimize_with_lb_tree_search": {
   "default": "false",
   "doc": "Do a more conventional tree search (by opposition to SAT based one) where we keep all the explored node in a tree. This is meant to be used in a portfolio and focus on improving the objective lower bound. Keeping the whole tree allow us to report a better objective lower bound coming from the worst open node in the tree.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "optimize_with_max_hs": {
   "default": "false",
   "doc": "This has no effect if optimize_with_core is false. If true, use a different core-based algorithm similar to the max-HS algo for max-SAT. This is a hybrid MIP/CP approach and it uses a MIP solver in addition to the CP/SAT one. This is also related to the PhD work of tobyodavies@ \"Automatic Logic-Based Benders Decomposition with MiniZinc\" http://aaai.org/ocs/index.php/AAAI/AAAI17/paper/view/14489",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "output_drat_proof": {
   "default": "false",
   "doc": "If true, a DRAT proof that all the clauses inferred by the solver are valid\nis output to a file. As of December 2025, this only works for pure SAT\nproblems, with\n - num_workers = 1,\n - cp_model_presolve = false,\n - linearization_level <= 1,\n - symmetry_level <= 1.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "bool"
  },
  "output_lrat_proof": {
   "default": "false",
   "doc": "If true, an LRAT proof that all the clauses inferred by the solver are\nvalid is output to several files (one for presolve -- reduced to trivial\nsimplifications if cp_model_presolve is false, one per worker, and one for\nthe merged proof). As of December 2025, this only works for pure SAT\nproblems, with\n - cp_model_presolve = false,\n - linearization_level <= 1,\n - symmetry_level <= 1.",
   "enum": null,
   "repeated": false,
   "section": "Proofs",
   "type": "bool"
  },
  "pb_cleanup_increment": {
   "default": "200",
   "doc": "Same as for the clauses, but for the learned pseudo-Boolean constraints.",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "int32"
  },
  "pb_cleanup_ratio": {
   "default": "0.5",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Clause database management",
   "type": "double"
  },
  "permute_presolve_constraint_order": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "permute_variable_randomly": {
   "default": "false",
   "doc": "This is mainly here to test the solver variability. Note that in tests, if not explicitly set to false, all 3 options will be set to true so that clients do not rely on the solver returning a specific solution if they are many equivalent optimal solutions.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "polarity_exploit_ls_hints": {
   "default": "false",
   "doc": "If true and we have first solution LS workers, tries in some phase to follow a LS solutions that violates has litle constraints as possible.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "bool"
  },
  "polarity_rephase_increment": {
   "default": "1000",
   "doc": "If non-zero, then we change the polarity heuristic after that many number of conflicts in an arithmetically increasing fashion. So x the first time, 2 * x the second time, etc...",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "int32"
  },
  "polish_lp_solution": {
   "default": "false",
   "doc": "Whether we try to do a few degenerate iteration at the end of an LP solve to minimize the fractionality of the integer variable in the basis. This helps on some problems, but not so much on others. It also cost of bit of time to do such polish step.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "preferred_variable_order": {
   "default": "IN_ORDER",
   "doc": "",
   "enum": "VariableOrder",
   "repeated": false,
   "section": "Branching and polarity",
   "type": "VariableOrder"
  },
  "presolve_blocked_clause": {
   "default": "true",
   "doc": "Whether we use an heuristic to detect some basic case of blocked clause in the SAT presolve.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "presolve_bva_threshold": {
   "default": "1",
   "doc": "Apply Bounded Variable Addition (BVA) if the number of clauses is reduced by stricly more than this threshold. The algorithm described in the paper uses 0, but quick experiments showed that 1 is a good value. It may not be worth it to add a new variable just to remove one clause.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "presolve_bve_clause_weight": {
   "default": "3",
   "doc": "During presolve, we apply BVE only if this weight times the number of clauses plus the number of clause literals is not increased.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "presolve_bve_threshold": {
   "default": "500",
   "doc": "During presolve, only try to perform the bounded variable elimination (BVE) of a variable x if the number of occurrences of x times the number of occurrences of not(x) is not greater than this parameter.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "presolve_extract_integer_enforcement": {
   "default": "false",
   "doc": "If true, we will extract from linear constraints, enforcement literals of the form \"integer variable at bound => simplified constraint\". This should always be beneficial except that we don't always handle them as efficiently as we could for now. This causes problem on manna81.mps (LP relaxation not as tight it seems) and on neos-3354841-apure.mps.gz (too many literals created this way).",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "presolve_inclusion_work_limit": {
   "default": "100000000",
   "doc": "A few presolve operations involve detecting constraints included in other constraint. Since there can be a quadratic number of such pairs, and processing them usually involve scanning them, the complexity of these operations can be big. This enforce a local deterministic limit on the number of entries scanned. Default is 1e8.\n\nA value of zero will disable these presolve rules completely.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int64"
  },
  "presolve_probing_deterministic_time_limit": {
   "default": "30.0",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "double"
  },
  "presolve_substitution_level": {
   "default": "1",
   "doc": "How much substitution (also called free variable aggregation in MIP litterature) should we perform at presolve. This currently only concerns variable appearing only in linear constraints. For now the value 0 turns it off and any positive value performs substitution.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "presolve_use_bva": {
   "default": "true",
   "doc": "Whether or not we use Bounded Variable Addition (BVA) in the presolve.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "probing_deterministic_time_limit": {
   "default": "1.0",
   "doc": "The maximum \"deterministic\" time limit to spend in probing. A value of zero will disable the probing.\n\nTODO(user): Clean up. The first one is used in CP-SAT, the other in pure SAT presolve.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "double"
  },
  "probing_num_combinations_limit": {
   "default": "20000",
   "doc": "How many combinations of pairs or triplets of variables we want to scan.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "propagation_loop_detection_factor": {
   "default": "10.0",
   "doc": "Some search decisions might cause a really large number of propagations to happen when integer variables with large domains are only reduced by 1 at each step. If we propagate more than the number of variable times this parameters we try to take counter-measure. Setting this to 0.0 disable this feature.\n\nTODO(user): Setting this to something like 10 helps in most cases, but the code is currently buggy and can cause the solve to enter a bad state where no progress is made.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "pseudo_cost_reliability_threshold": {
   "default": "100",
   "doc": "The solver ignores the pseudo costs of variables with number of recordings less than this threshold.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int64"
  },
  "push_all_tasks_toward_start": {
   "default": "false",
   "doc": "Experimental code: specify if the objective pushes all tasks toward the start of the schedule.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "random_branches_ratio": {
   "default": "0.0",
   "doc": "A number between 0 and 1 that indicates the proportion of branching variables that are selected randomly instead of choosing the first variable from the given variable_ordering strategy.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "double"
  },
  "random_polarity_ratio": {
   "default": "0.0",
   "doc": "The proportion of polarity chosen at random. Note that this take precedence over the phase saving heuristic. This is different from initial_polarity:POLARITY_RANDOM because it will select a new random polarity each time the variable is branched upon instead of selecting one initially and then always taking this choice.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "double"
  },
  "random_seed": {
   "default": "1",
   "doc": "At the beginning of each solve, the random number generator used in some part of the solver is reinitialized to this seed. If you change the random seed, the solver may make different choices during the solving process.\n\nFor some problems, the running time may vary a lot depending on small change in the solving algorithm. Running the solver with different seeds enables to have more robust benchmarks when evaluating new features.",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "int32"
  },
  "randomize_search": {
   "default": "false",
   "doc": "Randomize fixed search.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "relative_gap_limit": {
   "default": "0.0",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Limits",
   "type": "double"
  },
  "remove_fixed_variables_early": {
   "default": "true",
   "doc": "If cp_model_presolve is true and there is a large proportion of fixed variable after the first model copy, remap all the model to a dense set of variable before the full presolve even starts. This should help for LNS on large models.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "bool"
  },
  "repair_hint": {
   "default": "false",
   "doc": "If true, the solver tries to repair the solution given in the hint. This search terminates after the 'hint_conflict_limit' is reached and the solver switches to regular search. If false, then  we do a FIXED_SEARCH using the hint until the hint_conflict_limit is reached.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "restart_algorithms": {
   "default": null,
   "doc": "The restart strategies will change each time the strategy_counter is increased. The current strategy will simply be the one at index strategy_counter modulo the number of strategy. Note that if this list includes a NO_RESTART, nothing will change when it is reached because the strategy_counter will only increment after a restart.\n\nThe idea of switching of search strategy tailored for SAT/UNSAT comes from Chanseok Oh with his COMiniSatPS solver, see http://cs.nyu.edu/~chanseok/. But more generally, it seems REALLY beneficial to try different strategy.",
   "enum": "RestartAlgorithm",
   "repeated": true,
   "section": "Restart",
   "type": "RestartAlgorithm"
  },
  "restart_dl_average_ratio": {
   "default": "1.0",
   "doc": "In the moving average restart algorithms, a restart is triggered if the window average times this ratio is greater that the global average.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "double"
  },
  "restart_lbd_average_ratio": {
   "default": "1.0",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "double"
  },
  "restart_period": {
   "default": "50",
   "doc": "Restart period for the FIXED_RESTART strategy. This is also the multiplier used by the LUBY_RESTART strategy.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "int32"
  },
  "restart_running_window_size": {
   "default": "50",
   "doc": "Size of the window for the moving average restarts.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "int32"
  },
  "root_lp_iterations": {
   "default": "2000",
   "doc": "Even at the root node, we do not want to spend too much time on the LP if it is \"difficult\". So we solve it in \"chunks\" of that many iterations. The solve will be continued down in the tree or the next time we go back to the root node.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "int32"
  },
  "routing_cut_dp_effort": {
   "default": "1e7",
   "doc": "The amount of \"effort\" to spend in dynamic programming for computing routing cuts. This is in term of basic operations needed by the algorithm in the worst case, so a value like 1e8 should take less than a second to compute.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "routing_cut_max_infeasible_path_length": {
   "default": "6",
   "doc": "If the length of an infeasible path is less than this value, a cut will be added to exclude it.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "routing_cut_subset_size_for_binary_relation_bound": {
   "default": "0",
   "doc": "If the size of a subset of nodes of a RoutesConstraint is less than this value, use linear constraints of size 1 and 2 (such as capacity and time window constraints) enforced by the arc literals to compute cuts for this subset (unless the subset size is less than routing_cut_subset_size_for_tight_binary_relation_bound, in which case the corresponding algorithm is used instead). The algorithm for these cuts has a O(n^3) complexity, where n is the subset size. Hence the value of this parameter should not be too large (e.g. 10 or 20).",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "routing_cut_subset_size_for_exact_binary_relation_bound": {
   "default": "8",
   "doc": "Similar to above, but with an even stronger algorithm in O(n!). We try to be defensive and abort early or not run that often. Still the value of that parameter shouldn't really be much more than 10.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "routing_cut_subset_size_for_shortest_paths_bound": {
   "default": "8",
   "doc": "Similar to routing_cut_subset_size_for_exact_binary_relation_bound but use a bound based on shortest path distances (which respect triangular inequality). This allows to derive bounds that are valid for any superset of a given subset. This is slow, so it shouldn't really be larger than 10.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "routing_cut_subset_size_for_tight_binary_relation_bound": {
   "default": "0",
   "doc": "Similar to above, but with a different algorithm producing better cuts, at the price of a higher O(2^n) complexity, where n is the subset size. Hence the value of this parameter should be small (e.g. less than 10).",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "save_lp_basis_in_lb_tree_search": {
   "default": "false",
   "doc": "Experimental. Save the current LP basis at each node of the search tree so that when we jump around, we can load it and reduce the number of LP iterations needed.\n\nIt currently works okay if we do not change the lp with cuts or simplification... More work is needed to make it robust in all cases.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "search_branching": {
   "default": "AUTOMATIC_SEARCH",
   "doc": "",
   "enum": "SearchBranching",
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "SearchBranching"
  },
  "search_random_variable_pool_size": {
   "default": "0",
   "doc": "Search randomization will collect the top 'search_random_variable_pool_size' valued variables, and pick one randomly. The value of the variable is specific to each strategy.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int64"
  },
  "share_binary_clauses": {
   "default": "true",
   "doc": "Allows sharing of new learned binary clause between workers.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "share_glue_clauses": {
   "default": "true",
   "doc": "Allows sharing of short glue clauses between workers. Implicitly disabled if share_binary_clauses is false.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "share_glue_clauses_dtime": {
   "default": "1.0",
   "doc": "The amount of dtime between each export of shared glue clauses.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "double"
  },
  "share_level_zero_bounds": {
   "default": "true",
   "doc": "Allows sharing of the bounds of modified variables at level 0.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "share_linear2_bounds": {
   "default": "false",
   "doc": "Allows sharing of the bounds on linear2 discovered at level 0. This is mainly interesting on scheduling type of problems when we branch on precedences.\n\nWarning: This currently non-deterministic.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "share_objective_bounds": {
   "default": "true",
   "doc": "Allows objective sharing between workers.",
   "enum": null,
   "repeated": false,
   "section": "Multithread",
   "type": "bool"
  },
  "shared_tree_balance_tolerance": {
   "default": "1",
   "doc": "How much deeper compared to the ideal max depth of the tree is considered \"balanced\" enough to still accept a split. Without such a tolerance, sometimes the tree can only be split by a single worker, and they may not generate a split for some time. In contrast, with a tolerance of 1, at least half of all workers should be able to split the tree as soon as a split becomes required. This only has an effect on SPLIT_STRATEGY_BALANCED_TREE and SPLIT_STRATEGY_DISCREPANCY.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "shared_tree_max_nodes_per_worker": {
   "default": "10000",
   "doc": "In order to limit total shared memory and communication overhead, limit the total number of nodes that may be generated in the shared tree. If the shared tree runs out of unassigned leaves, workers act as portfolio workers. Note: this limit includes interior nodes, not just leaves.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "shared_tree_num_workers": {
   "default": "-1",
   "doc": "Enables shared tree search. If positive, start this many complete worker threads to explore a shared search tree. These workers communicate objective bounds and simple decision nogoods relating to the shared prefix of the tree, and will avoid exploring the same subtrees as one another. Specifying a negative number uses a heuristic to select an appropriate number of shared tree workeres based on the total number of workers.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "shared_tree_open_leaves_per_worker": {
   "default": "2.0",
   "doc": "How many open leaf nodes should the shared tree maintain per worker.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "shared_tree_split_min_dtime": {
   "default": "0.1",
   "doc": "How much dtime a worker will wait between proposing splits. This limits the contention in splitting the shared tree, and also reduces the number of too-easy subtrees that are generates.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "shared_tree_split_strategy": {
   "default": "SPLIT_STRATEGY_AUTO",
   "doc": "",
   "enum": "SharedTreeSplitStrategy",
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "SharedTreeSplitStrategy"
  },
  "shared_tree_worker_enable_phase_sharing": {
   "default": "true",
   "doc": "If true, shared tree workers share their target phase when returning an assigned subtree for the next worker to use.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "shared_tree_worker_enable_trail_sharing": {
   "default": "true",
   "doc": "If true, workers share more of the information from their local trail. Specifically, literals implied by the shared tree decisions.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "shared_tree_worker_min_restarts_per_subtree": {
   "default": "1",
   "doc": "Minimum restarts before a worker will replace a subtree that looks \"bad\" based on the average LBD of learned clauses.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "shaving_deterministic_time_in_probing_search": {
   "default": "0.001",
   "doc": "Add a shaving phase (where the solver tries to prove that the lower or upper bound of a variable are infeasible) to the probing search. (<= 0 disables it).",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "shaving_search_deterministic_time": {
   "default": "0.1",
   "doc": "Specifies the amount of deterministic time spent of each try at shaving a bound in the shaving search.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "shaving_search_threshold": {
   "default": "64",
   "doc": "Specifies the threshold between two modes in the shaving procedure. If the range of the variable/objective is less than this threshold, then the shaving procedure will try to remove values one by one. Otherwise, it will try to remove one range at a time.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int64"
  },
  "solution_pool_diversity_limit": {
   "default": "10",
   "doc": "If solution_pool_size is <= this, we will use DP to keep a \"diverse\" set of solutions (the one further apart via hamming distance) in the pool. Setting this to large value might be slow, especially if your solution are large.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "solution_pool_size": {
   "default": "3",
   "doc": "Size of the top-n different solutions kept by the solver. This parameter must be > 0. Currently, having this larger than one mainly impact the \"base\" solution chosen for a LNS/LS fragment.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "stop_after_first_solution": {
   "default": "false",
   "doc": "For an optimization problem, stop the solver as soon as we have a solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "stop_after_presolve": {
   "default": "false",
   "doc": "Mainly used when improving the presolver. When true, stops the solver after the presolve is complete (or after loading and root level propagation).",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "stop_after_root_propagation": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "strategy_change_increase_ratio": {
   "default": "0.0",
   "doc": "The parameter num_conflicts_before_strategy_changes is increased by that much after each strategy change.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "double"
  },
  "subsolver_params": {
   "default": null,
   "doc": "It is possible to specify additional subsolver configuration. These can be referred by their params.name() in the fields above. Note that only the specified field will \"overwrite\" the ones of the base parameter. If a subsolver_params has the name of an existing subsolver configuration, the named parameters will be merged into the subsolver configuration.",
   "enum": null,
   "repeated": true,
   "section": "Multithread",
   "type": "SatParameters"
  },
  "subsolvers": {
   "default": null,
   "doc": "In multi-thread, the solver can be mainly seen as a portfolio of solvers with different parameters. This field indicates the names of the parameters that are used in multithread. This only applies to \"full\" subsolvers.\n\nSee cp_model_search.cc to see a list of the names and the default value (if\nleft empty) that looks like:\n- default_lp           (linearization_level:1)\n- fixed                (only if fixed search specified or scheduling)\n- no_lp                (linearization_level:0)\n- max_lp               (linearization_level:2)\n- pseudo_costs         (only if objective, change search heuristic)\n- reduced_costs        (only if objective, change search heuristic)\n- quick_restart        (kind of probing)\n- quick_restart_no_lp  (kind of probing with linearization_level:0)\n- lb_tree_search       (to improve lower bound, MIP like tree search)\n- probing              (continuous probing and shaving)\n\nAlso, note that some set of parameters will be ignored if they do not make sense. For instance if there is no objective, pseudo_cost or reduced_cost search will be ignored. Core based search will only work if the objective has many terms. If there is no fixed strategy fixed will be ignored. And so on.\n\nThe order is important, as only the first num_full_subsolvers will be scheduled. You can see in the log which one are selected for a given run.",
   "enum": null,
   "repeated": true,
   "section": "Multithread",
   "type": "string"
  },
  "subsume_during_vivification": {
   "default": "true",
   "doc": "If we remove clause that we now are \"implied\" by others. Note that this might not always be good as we might loose some propagation power.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "bool"
  },
  "subsumption_during_conflict_analysis": {
   "default": "true",
   "doc": "At a really low cost, during the 1-UIP conflict computation, it is easy to detect if some of the involved reasons are subsumed by the current conflict. When this is true, such clauses are detached and later removed from the problem.",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "bool"
  },
  "symmetry_detection_deterministic_time_limit": {
   "default": "1.0",
   "doc": "Deterministic time limit for symmetry detection.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "symmetry_level": {
   "default": "2",
   "doc": "Whether we try to automatically detect the symmetries in a model and exploit them. Currently, at level 1 we detect them in presolve and try to fix Booleans. At level 2, we also do some form of dynamic symmetry breaking during search. At level 3, we also detect symmetries for very large models, which can be slow. At level 4, we try to break as much symmetry as possible in presolve.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "table_compression_level": {
   "default": "2",
   "doc": "How much we try to \"compress\" a table constraint. Compressing more leads to less Booleans and faster propagation but can reduced the quality of the lp relaxation. Values goes from 0 to 3 where we always try to fully compress a table. At 2, we try to automatically decide if it is worth it.",
   "enum": null,
   "repeated": false,
   "section": "Presolve",
   "type": "int32"
  },
  "transitive_precedences_work_limit": {
   "default": "1000000",
   "doc": "At root level, we might compute the transitive closure of \"precedences\" relations so that we can exploit that in scheduling problems. Setting this to zero disable the feature.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "use_absl_random": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "use_all_different_for_circuit": {
   "default": "false",
   "doc": "Turn on extra propagation for the circuit constraint. This can be quite slow.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_area_energetic_reasoning_in_no_overlap_2d": {
   "default": "false",
   "doc": "When this is true, the no_overlap_2d constraint is reinforced with an energetic reasoning that uses an area-based energy. This can be combined with the two other overlap heuristics above.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_blocking_restart": {
   "default": "false",
   "doc": "Block a moving restart algorithm if the trail size of the current conflict is greater than the multiplier times the moving average of the trail size at the previous conflicts.",
   "enum": null,
   "repeated": false,
   "section": "Restart",
   "type": "bool"
  },
  "use_chronological_backtracking": {
   "default": "false",
   "doc": "If true, try to backtrack as little as possible on conflict and re-imply the clauses later. This means we discard less propagation than traditional backjumping, but requites additional bookkeeping to handle reimplication. See: https://doi.org/10.1007/978-3-319-94144-8_7",
   "enum": null,
   "repeated": false,
   "section": "Conflict analysis",
   "type": "bool"
  },
  "use_combined_no_overlap": {
   "default": "false",
   "doc": "This can be beneficial if there is a lot of no-overlap constraints but a relatively low number of different intervals in the problem. Like 1000 intervals, but 1M intervals in the no-overlap constraints covering them.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_conservative_scale_overload_checker": {
   "default": "false",
   "doc": "Enable a heuristic to solve cumulative constraints using a modified energy constraint. We modify the usual energy definition by applying a super-additive function (also called \"conservative scale\" or \"dual-feasible function\") to the demand and the durations of the tasks.\n\nThis heuristic is fast but for most problems it does not help much to find a solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_disjunctive_constraint_in_cumulative": {
   "default": "true",
   "doc": "When this is true, the cumulative constraint is reinforced with propagators from the disjunctive constraint to improve the inference on a set of tasks that are disjunctive at the root of the problem. This additional level supplements the default level of reasoning.\n\nPropagators of the cumulative constraint will not be used at all if all the tasks are disjunctive at root node.\n\nThis always result in better propagation, but it is usually slow, so depending on the problem, turning this off may lead to a faster solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_dual_scheduling_heuristics": {
   "default": "true",
   "doc": "When set, it activates a few scheduling parameters to improve the lower bound of scheduling problems. This is only effective with multiple workers as it modifies the reduced_cost, lb_tree_search, and probing workers.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_dynamic_precedence_in_cumulative": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_dynamic_precedence_in_disjunctive": {
   "default": "false",
   "doc": "Whether we try to branch on decision \"interval A before interval B\" rather than on intervals bounds. This usually works better, but slow down a bit the time to find the first solution.\n\nThese parameters are still EXPERIMENTAL, the result should be correct, but it some corner cases, they can cause some failing CHECK in the solver.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_energetic_reasoning_in_no_overlap_2d": {
   "default": "false",
   "doc": "When this is true, the no_overlap_2d constraint is reinforced with energetic reasoning. This additional level supplements the default level of reasoning.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_erwa_heuristic": {
   "default": "false",
   "doc": "Whether we use the ERWA (Exponential Recency Weighted Average) heuristic as described in \"Learning Rate Based Branching Heuristic for SAT solvers\", J.H.Liang, V. Ganesh, P. Poupart, K.Czarnecki, SAT 2016.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "bool"
  },
  "use_exact_lp_reason": {
   "default": "true",
   "doc": "The solver usually exploit the LP relaxation of a model. If this option is true, then whatever is infered by the LP will be used like an heuristic to compute EXACT propagation on the IP. So with this option, there is no numerical imprecision issues.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_extended_probing": {
   "default": "true",
   "doc": "Use extended probing (probe bool_or, at_most_one, exactly_one).",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_feasibility_jump": {
   "default": "true",
   "doc": "Parameters for an heuristic similar to the one described in the paper: \"Feasibility Jump: an LP-free Lagrangian MIP heuristic\", Bj\u00f8rnar Luteberget, Giorgio Sartor, 2023, Mathematical Programming Computation.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_feasibility_pump": {
   "default": "true",
   "doc": "Adds a feasibility pump subsolver along with lns subsolvers.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_hard_precedences_in_cumulative": {
   "default": "false",
   "doc": "If true, detect and create constraint for integer variable that are \"after\" a set of intervals in the same cumulative constraint.\n\nExperimental: by default we just use \"direct\" precedences. If exploit_all_precedences is true, we explore the full precedence graph. This assumes we have a DAG otherwise it fails.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_implied_bounds": {
   "default": "true",
   "doc": "Stores and exploits \"implied-bounds\" in the solver. That is, relations of the form literal => (var >= bound). This is currently used to derive stronger cuts.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_lb_relax_lns": {
   "default": "true",
   "doc": "Turns on neighborhood generator based on local branching LP. Based on Huang et al., \"Local Branching Relaxation Heuristics for Integer Linear Programs\", 2023.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_linear3_for_no_overlap_2d_precedences": {
   "default": "true",
   "doc": "When set, this activates a propagator for the no_overlap_2d constraint that uses any eventual linear constraints of the model in the form `{start interval 1} - {end interval 2} + c*w <= ub` to detect that two intervals must overlap in one dimension for some values of `w`. This is particularly useful for problems where the distance between two boxes is part of the model.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_lns": {
   "default": "true",
   "doc": "Testing parameters used to disable all lns workers.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_lns_only": {
   "default": "false",
   "doc": "Experimental parameters to disable everything but lns.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_ls_only": {
   "default": "false",
   "doc": "Disable every other type of subsolver, setting this turns CP-SAT into a pure local-search solver.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_new_integer_conflict_resolution": {
   "default": "false",
   "doc": "This should be better on integer problems. But it is still work in progress.",
   "enum": null,
   "repeated": false,
   "section": "Linear programming relaxation",
   "type": "bool"
  },
  "use_objective_lb_search": {
   "default": "false",
   "doc": "If true, search will search in ascending max objective value (when minimizing) starting from the lower bound of the objective.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_objective_shaving_search": {
   "default": "false",
   "doc": "This search differs from the previous search as it will not use assumptions to bound the objective, and it will recreate a full model with the hardcoded objective value.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_optimization_hints": {
   "default": "true",
   "doc": "For an optimization problem, whether we follow some hints in order to find a better first solution. For a variable with hint, the solver will always try to follow the hint. It will revert to the variable_branching default otherwise.",
   "enum": null,
   "repeated": false,
   "section": "Max-sat parameters",
   "type": "bool"
  },
  "use_optional_variables": {
   "default": "false",
   "doc": "If true, we automatically detect variables whose constraint are always enforced by the same literal and we mark them as optional. This allows to propagate them as if they were present in some situation.\n\nTODO(user): This is experimental and seems to lead to wrong optimal in some situation. It should however gives correct solutions. Fix.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_overload_checker_in_cumulative": {
   "default": "false",
   "doc": "When this is true, the cumulative constraint is reinforced with overload checking, i.e., an additional level of reasoning based on energy. This additional level supplements the default level of reasoning as well as timetable edge finding.\n\nThis always result in better propagation, but it is usually slow, so depending on the problem, turning this off may lead to a faster solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_pb_resolution": {
   "default": "false",
   "doc": "Experimental.\n\nThis is an old experiment, it might cause crashes in multi-thread and you should double check the solver result. It can still be used if you only care about feasible solutions (these are checked) and it gives good result on your problem. We might revive it at some point.\n\nWhether to use pseudo-Boolean resolution to analyze a conflict. Note that this option only make sense if your problem is modelized using pseudo-Boolean constraints. If you only have clauses, this shouldn't change anything (except slow the solver down).",
   "enum": null,
   "repeated": false,
   "section": "Other parameters",
   "type": "bool"
  },
  "use_phase_saving": {
   "default": "true",
   "doc": "If this is true, then the polarity of a variable will be the last value it was assigned to, or its default polarity if it was never assigned since the call to ResetDecisionHeuristic().\n\nActually, we use a newer version where we follow the last value in the longest non-conflicting partial assignment in the current phase.\n\nThis is called 'literal phase saving'. For details see 'A Lightweight Component Caching Scheme for Satisfiability Solvers' K. Pipatsrisawat and A.Darwiche, In 10th International Conference on Theory and Applications of Satisfiability Testing, 2007.",
   "enum": null,
   "repeated": false,
   "section": "Branching and polarity",
   "type": "bool"
  },
  "use_precedences_in_disjunctive_constraint": {
   "default": "true",
   "doc": "When this is true, then a disjunctive constraint will try to use the precedence relations between time intervals to propagate their bounds further. For instance if task A and B are both before C and task A and B are in disjunction, then we can deduce that task C must start after duration(A) + duration(B) instead of simply max(duration(A), duration(B)), provided that the start time for all task was currently zero.\n\nThis always result in better propagation, but it is usually slow, so depending on the problem, turning this off may lead to a faster solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_probing_search": {
   "default": "false",
   "doc": "If true, search will continuously probe Boolean variables, and integer variable bounds. This parameter is set to true in parallel on the probing worker.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_rins_lns": {
   "default": "true",
   "doc": "Turns on relaxation induced neighborhood generator.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_sat_inprocessing": {
   "default": "true",
   "doc": "Enable or disable \"inprocessing\" which is some SAT presolving done at each restart to the root level.",
   "enum": null,
   "repeated": false,
   "section": "Inprocessing",
   "type": "bool"
  },
  "use_shared_tree_search": {
   "default": "false",
   "doc": "Set on shared subtree workers. Users should not set this directly.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_strong_propagation_in_disjunctive": {
   "default": "false",
   "doc": "Enable stronger and more expensive propagation on no_overlap constraint.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_symmetry_in_lp": {
   "default": "false",
   "doc": "When we have symmetry, it is possible to \"fold\" all variables from the same orbit into a single variable, while having the same power of LP relaxation. This can help significantly on symmetric problem. However there is currently a bit of overhead as the rest of the solver need to do some translation between the folded LP and the rest of the problem.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_timetable_edge_finding_in_cumulative": {
   "default": "false",
   "doc": "When this is true, the cumulative constraint is reinforced with timetable edge finding, i.e., an additional level of reasoning based on the conjunction of energy and mandatory parts. This additional level supplements the default level of reasoning as well as overload_checker.\n\nThis always result in better propagation, but it is usually slow, so depending on the problem, turning this off may lead to a faster solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_timetabling_in_no_overlap_2d": {
   "default": "false",
   "doc": "When this is true, the no_overlap_2d constraint is reinforced with propagators from the cumulative constraints. It consists of ignoring the position of rectangles in one position and projecting the no_overlap_2d on the other dimension to create a cumulative constraint. This is done on both axis. This additional level supplements the default level of reasoning.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "use_try_edge_reasoning_in_no_overlap_2d": {
   "default": "false",
   "doc": "",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "bool"
  },
  "variable_activity_decay": {
   "default": "0.8",
   "doc": "Each time a conflict is found, the activities of some variables are increased by one. Then, the activity of all variables are multiplied by variable_activity_decay.\n\nTo implement this efficiently, the activity of all the variables is not decayed at each conflict. Instead, the activity increment is multiplied by 1 / decay. When an activity reach max_variable_activity_value, all the activity are multiplied by 1 / max_variable_activity_value.",
   "enum": null,
   "repeated": false,
   "section": "Variable and clause activities",
   "type": "double"
  },
  "variables_shaving_level": {
   "default": "-1",
   "doc": "This search takes all Boolean or integer variables, and maximize or minimize them in order to reduce their domain. -1 is automatic, otherwise value 0 disables it, and 1, 2, or 3 changes something.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  },
  "violation_ls_compound_move_probability": {
   "default": "0.5",
   "doc": "Probability of using compound move search each restart. TODO(user): Add reference to paper when published.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "double"
  },
  "violation_ls_perturbation_period": {
   "default": "100",
   "doc": "How long violation_ls should wait before perturbating a solution.",
   "enum": null,
   "repeated": false,
   "section": "Constraint programming parameters",
   "type": "int32"
  }
 },
 "sections": [
  "Branching and polarity",
  "Conflict analysis",
  "Clause database management",
  "Variable and clause activities",
  "Restart",
  "Limits",
  "Other parameters",
  "Presolve",
  "Inprocessing",
  "Multithread",
  "Proofs",
  "Debugging parameters",
  "Max-sat parameters",
  "Constraint programming parameters",
  "Linear programming relaxation"
 ],
 "source": "ortools/sat/sat_parameters.proto (/home/krupke/Repositories/or-tools/ortools/sat/sat_parameters.proto)"
}
